// Big O: O(nˆ2)
// Explanation: The outer loop is O(n) and the inner loop is O(n) so we multiply them to get O(nˆ2).

function quadraticFunc(num){
    for(let i = 0; i < num; i++){
        for(let j = 0; j < num; j++){
        }
    }
}

quadraticFunc(5);