// Big O: O(n)
// Explanation: The loop is O(n) because it is iterating through the array.

function linearFunc(num){
    for(let i = 0; i < num; i++){ 
        console.log(i) 
    }
}

linearFunc(10);